target-version = "py311"
line-length = 120

# Set de reglas enfocado en bugs reales (imports/nombres no definidos,
# errores de sintaxis), no en estilo ni modernizacion de tipos, para no
# generar ruido en el codigo existente.
[lint]
select = ["E4", "E7", "E9", "F"]
